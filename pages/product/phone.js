// when access "/product/phone" nextJS will prioritize selection by file name
// before finding dynamic routing same as "/product/:productId"
const Phone = () => {
  return (
    <div>Phone page</div>
  )
}

export default Phone;